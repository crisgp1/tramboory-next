// Barrel export file to map current component structure to expected import paths
// This facilitates imports like '@/features/home/components' which map to existing components

// Export sections components (HeroSection, ServicesSection, FeaturesSection, etc.)
export * from '../../../components/home/sections';

// Export decorative components (ParticlesBackground, AnimatedBalloons, BackgroundVideo)
export * from '../../../components/decorative';

// Export UI components
export * from '../../../components/ui';