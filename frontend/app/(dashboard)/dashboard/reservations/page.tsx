/**
 * Página de Gestión de Reservas
 * @module ReservationsPage
 * @description Página principal para la gestión de reservas del sistema
 */

import { ReservationsMain } from '@/components/dashboard'

/**
 * ReservationsPage - Página de gestión de reservas
 *
 * Importa y renderiza el componente ReservationsMain que contiene
 * toda la lógica y UI para la gestión de reservas del sistema.
 */
export default function ReservationsPage() {
  return <ReservationsMain />
}