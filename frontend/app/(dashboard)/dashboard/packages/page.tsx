/**
 * Página de Gestión de Paquetes
 * @module PackagesPage
 * @description Página principal para la gestión de paquetes del sistema
 */

import { PackagesMain } from '@/components/dashboard'

export default function PackagesPage() {
  return <PackagesMain />
}